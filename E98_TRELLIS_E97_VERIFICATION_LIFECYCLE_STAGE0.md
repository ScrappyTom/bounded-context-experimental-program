# E98 — Trellis E97 verification-lifecycle scout Stage 0

Date: 2026-08-29

Source apparatus commit:
[`520d82380be3498b2330093535995342a9811de7`](https://github.com/ScrappyTom/qwen38-artifact-coupled-integration-scout-v0/commit/520d82380be3498b2330093535995342a9811de7)

## Program question

E97 proved that delivered mutation action/effect pairs can be compacted after exact lineage shows that the current artifact contains them. E98 turns that mechanism into one bounded behavioral question:

> Can the integrated system deliver the exact pending effect, remain oriented to the current artifact, enter current verification, repair independently frozen defects incrementally, recheck the changed candidate, and stop correctly?

This is not another component qualification. The live causal unit contains the existing lossy semantic scaffold, exact evidence ledger, exact incremental candidate, E97 applied-history lifecycle, pending effect, bounded current check, repair transport, recheck, and closure proposal.

## Donor integrity

The sealed E96 V1 checkpoint is not directly resumed under changed code. E98 verifies it and imports the event state immediately before its historical `capacity_blocked` terminal into a new execution manifest. The migration receipt preserves the donor checkpoint/configuration, excluded terminal event, candidate and event-prefix hashes, 29 inherited provider attempts, 350,510 inherited serialized tokens, and exact new-checkpoint parentage.

## Frozen readiness and activation

Before future model behavior, the donor candidate is independently `not_ready`: 904 words, missing its sixth required section, below required decision source breadth, and failing all eight substantive relation groups.

E97 externalizes only delivered `RESULT-013` through `RESULT-017` and their causal actions. Pending `RESULT-018` remains exact. The offline packet is 19,116 tokens against a 20,992-token prompt allowance, and the first future completed request must include that result plus exact current-candidate and current-effect bindings.

## Verification representation

The first provider-free pass found that the generic check body and replaceable verification slot duplicated verbose evaluator text. E98 freezes a bounded mechanical projection: raw evaluator output stays exact externally, while actor-visible findings contain candidate-bound failed criterion IDs, short descriptions, frozen expectations, readiness, and a raw-result handle. Candidate change makes prior findings explicitly stale. No finding is semantically repaired.

## Provider-free qualification

Across one exact checkpoint/resume boundary, the scripted system delivered the pending effect, completed construction, entered verification, ran a current failing check, admitted six candidate-bound section repairs, ran a current passing recheck, and submitted after that recheck.

The path used eleven actor calls, zero maintenance calls, and no GPU/provider calls. Four focused tests and the full 307-test apparatus regression pass; Ruff and scoped mypy pass.

## Frozen live limits

- one donor-derived configuration;
- at most 18 additional actor calls;
- at most one additional maintenance call;
- at most 19 additional provider calls;
- at most 450,000 additional serialized tokens;
- one attempt per call and zero retries;
- mandatory review after at most six additional actor calls;
- no automatic continuation.

The live run is selected but not authorized.

## Claim limits

Provider-free completion proves reachability, not Qwen behavior. A live positive would remain donor-derived and would earn fresh-world transfer, not architecture promotion. A negative routes from the first transcript-level failure—orientation, evaluator use, repair transport, currentness, recurrence, or closure—not from a presumption that receipt formatting needs more tuning.

Disposition: **AF/NQ** — a lifecycle-complete donor-derived live scout is frozen and awaits explicit GPU authorization.
