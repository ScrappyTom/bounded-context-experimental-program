# E73 — Orchard phase-conditional lifecycle offline Stage 0

Date: 2026-08-27

Source:
[`444ab65`](https://github.com/ScrappyTom/qwen38-artifact-coupled-integration-scout-v0/commit/444ab65)

## Disposition

The fresh whole-lifecycle transfer apparatus qualifies offline with zero model
calls. No behavioral or utility conclusion is authorized.

Orchard is a synthetic thirteen-source biologics-restart task with exact
authority, process, aseptic, cold-chain, utility, analytical, cyber, supply,
environmental, telemetry, communication, change-lineage, and readiness
relationships. Both future arms share anchored semantic maintenance and exact
incremental artifact construction until the same actor-invoked mechanical
construction milestone.

After that milestone:

```text
F0
  retain scaffold
  + append verification actions/checks/effects

P1
  externalize scaffold with exact handle
  + replace model-facing candidate/effect/check projection
  + preserve full exact history externally
```

This is a compound lifecycle comparison, not component isolation.

## Offline findings

The exact source corpus is 48,759 bytes and 17,189 tokenizer tokens. A legal
six-pair ingress path produces a 21,116-token packet while COMMUNE/CHANGE is
pending and before REVIEW is read. Externalizing only `RESULT-001` restores
18,473 tokens. This proves a plausible policy-activation opportunity, not a
realized actor trajectory.

Provider-free fixtures in both arms reach construction milestone, verification
transition, candidate-bound failing check, bounded exact repair, stale prior-
check binding, effect uptake, current passing recheck, and submission mechanics.
Both produce identical final bytes. P1's model-facing verification packet is
8,014 tokens after repair and 7,086 after current recheck, proving replacement
rather than hidden accumulation. F0 grows from 8,818 to 9,666 over the same
provider-free interval.

The checker catches four deliberately corrupted relationships: an observed
yield promoted to a sufficient one-batch gate, Grade A/Grade B limits reversed,
installed and usable power summed, and a superseded analytical limit promoted
to current. Expected/observed feedback remains a bounded mechanical surface,
not semantic truth or readiness authority.

All 178 standalone tests and the independent zero-call audit pass.

## Claim limits

E73 does not show that Qwen will realize pressure, create or use the scaffold,
construct incrementally, invoke the milestone, preserve continuity after P1
recomposition, repair correctly, or close a ready artifact. P1 also jointly
changes scaffold residency and verification-history lifecycle, so a future
difference cannot be attributed to either component alone.

## Next eligible operation

Only the treatment-free pressure screen may run next, under separate exact
commit-bound authorization:

* run ID `2026-08-27-orchard-phase-lifecycle-pressure-screen-v0`;
* at most 30 actor/provider calls;
* one attempt per call;
* zero retries.

The screen must deliver at least ten sources across ten domains, preserve an
unsubmitted candidate, and expose a source-externalizing feasible first-fit
fork. The F0/P1 runner is deliberately not frozen before that fork exists.
