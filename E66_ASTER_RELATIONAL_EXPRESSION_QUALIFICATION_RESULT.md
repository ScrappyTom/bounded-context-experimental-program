# E66 — Aster provenance-relational expression qualification

Date: 2026-08-26

Standalone result:
[`qwen38-artifact-coupled-integration-scout-v0@d19f45c`](https://github.com/ScrappyTom/qwen38-artifact-coupled-integration-scout-v0/commit/d19f45cd4478946f9271e85285df32f7152e8e6c)

Frozen inference commit:
`8aa9afbec32b5669755760f2d4d7b5c992150e05`

Run ID:
`2026-08-26-aster-relational-expression-qualification-v0`

## Literal result

The sole authorized call completed normally with one attempt and zero retries.
It consumed 4,428 prompt tokens and 708 completion tokens, for 5,136 serialized
tokens. The 707-token output contained four well-shaped source/version-bound
fact blocks: two for ANCHOR and two for BRIDGE.

The frozen transport admitted 0/4 claims. Each `EVIDENCE_QUOTE` was an exact
sentence substring inside one unique source line, but the carrier required the
complete source line. Since all four source lines were multi-sentence Markdown
lines, validation returned `evidence_quote_not_unique_exact_line`, then marked
both externalized sources unrepresented.

No retry, repair, normalization, register merge, actor continuation, or W0/L1
comparison occurred. Runtime release, exact tree sealing, prompt/output
bindings, provider custody, and independent audit passed.

## Separate semantic disposition

Direct frozen-source review found no fabricated exact value or unit, timing
conversion, authority transfer, stale promotion, relationship reversal,
absent-source mutation, readiness claim, or closure authorization in the raw
statements. The output correctly preserved restoration authority and its
verification condition, 1,800 milliseconds p95 observed lag, and the
fifteen-second RPO versus forty-five-minute RTO values.

Coverage was narrow: four of eight frozen distinctions were absent, two were
only partial, and no relationship claim was emitted. Under the frozen omission
rule this supports local raw material safety, not fidelity, transport, or
utility. Admission-based relevance failed because no claim entered the
register.

## Systems disposition

E66 distinguishes three properties:

```text
bounded grounded factual expression   local positive
raw semantic material safety          local positive
complete-line transport admission     local negative
relational expression in this output  untested
downstream persistence and utility    untested
```

Unlike E61, this does not expose a corpus-wide category error. The frozen
complete-line instruction was explicit; the model selected sentence-level
evidence instead. The result therefore closes this exact Aster carrier route
under its prospective stop rule. Another quote/parser/prompt micro-iteration
would make interface compliance, rather than system interaction, the research
object again.

The result does not reject provenance-local relational persistence generally.
It prevents the planned L1 arm from running on an unqualified carrier. No live
successor is selected automatically. The next route must be chosen from the
program-level interaction question, not represented as a repair or retry of
E66.

## Claim limits

- one task/world and one authentic externalization boundary;
- one model seed and one output;
- factual claims only in the observed output;
- zero mechanically admitted claims;
- no actor consumed a semantic register;
- no candidate changed and no artifact was evaluated; and
- no evidence about substitution, induced recovery, repair, readiness, or
  information-economic value.
