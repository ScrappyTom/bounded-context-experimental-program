# E50 — Cedar pressure screen v0 abort and v1 freeze

Date: 2026-08-24

Source:
[`ScrappyTom/qwen38-artifact-coupled-integration-scout-v0@952037a`](https://github.com/ScrappyTom/qwen38-artifact-coupled-integration-scout-v0/commit/952037afa399c0fd593605831d92f8988a81d4a4)

Disposition: **AF/NQ** — the authorized v0 identity is a sealed apparatus abort
with zero actor/provider calls. A mechanically identical v1 screen is frozen,
offline-qualified, and GPU-unauthorized.

## Literal event

The owner authorized run
`2026-08-25-cedar-ingress-aligned-pressure-screen-v0` at commit
`a48d67a8cd888fd54b9c7a59a7e8f1dcd094241f` for at most twenty calls, one
attempt per call, and zero retries.

Before launching the project model server, the runner's frozen exclusivity
guard found PID 17032 running a different llama.cpp executable, model package,
context, and port. The process was external to the repository. It was inspected
but not terminated. The runner failed closed before inference and sealed:

- the copied authorization receipt;
- exact freeze binding;
- passing runtime-asset verification;
- failure and finalization records; and
- the complete five-file run tree.

The v0 run contains zero actor calls, zero provider calls, no trajectory, no
screen result, and no project GPU-server start. It supplies no behavioral,
pressure, activation, or interaction evidence.

## Replacement freeze

The same run identity cannot be retried. The successor changes only:

```text
run ID: 2026-08-25-cedar-ingress-aligned-pressure-screen-v1
scope:  cedar_ingress_aligned_pressure_screen_v1
```

The Cedar task/world, model and runtime, seed 860251, twenty-call ceiling,
600,000-token ceiling, one-attempt/zero-retry rule, ordinary chronology,
ingress geometry, activation semantics, and stopping rules remain unchanged.
The placeholder handoff and downstream verifier now bind to v1.

Forty-one tests pass, including an exact seal/hash audit that proves v0 ended
before any provider or trajectory artifact. Offline preflight passes and v1 has
no run directory.

The external server subsequently exited without action from the project. That
removes the environmental blocker but does not transfer authorization. V1 at
commit `952037afa399c0fd593605831d92f8988a81d4a4` requires a new exact owner
authorization.
