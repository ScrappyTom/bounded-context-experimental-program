# E79 — Keystone bounded-causal pressure screen

Date: 2026-08-27

Standalone result commit:
`eacafe5e4b46fcb3ffbadd90e9823d16796f6f4e`

Run ID: `2026-08-27-keystone-bounded-causal-pressure-screen-v0`

## Result

The actor made nine valid, novel, complete-source reads with one attempt per
call and zero retries:

`MANDATE → TRACK → SIGNAL → POWER → ROLLING → CREW → WEATHER → FUEL → CYBER`

The first eight observations crossed later model decision boundaries. CYBER
was acquired by the host as pending `RESULT-009`, but ordinary delivery would
have required 22,267 prompt tokens against the 20,992 allowance. The exact
deficit was 1,275 tokens.

The frozen protected-pending first-fit counterfactual selects only
`RESULT-001`. Substituting an exact reopen receipt for its MANDATE body would
reduce the packet to 20,648 tokens, leaving 344 tokens of headroom.

The pressure fork did not qualify the intended V0/V1 interaction. Only eight
sources and eight evidence domains had been delivered to a later actor call,
below the prospectively frozen ten-source/ten-domain minima. Pending host
acquisition did not count as model delivery. The candidate remained the
10-word placeholder; no treatment, maintenance, construction, check, or
submission occurred. Nine calls consumed 102,009 serialized tokens.

## Systems interpretation

This is not a model-expression failure. Every action was valid, novel, and
coherent with the task's dependency structure. It is an activation-geometry
failure produced by the interaction among:

* one complete source per actor decision;
* roughly 5–6 KB exact source results;
* ordinary append-only residency before the fork;
* a ten-source/ten-domain interaction threshold; and
* a fixed 20,992-token prompt allowance.

The no-relief prefix reached capacity after eight delivered sources. The
planned interaction required ten. The first pressure event therefore cannot be
both the common no-relief screen endpoint and the intended bounded-causal
verification fork on this observed path.

The correct successor is not a lower evidence threshold chosen after the run
and not a measured continuation from this ineligible handoff. If Keystone is
retained, deterministic first-fit relief should become common pre-treatment
infrastructure. The actor must continue choosing evidence, and a later
prospectively frozen fork should require delivered evidence plus a real
construction/verification state. This keeps the unit as a whole interaction:

```text
actor-selected evidence ingress
× common deterministic relief
× semantic scaffold
× exact incremental artifact
× verification feedback
× bounded causal continuity
× repair / recheck / readiness / closure
```

## Audit and claim limit

Independent offline replay passes with zero failures. It verifies exact
authorization, model/task/commit bindings, nine provider receipts, action and
result re-execution, prompt counts, serialized-token accounting, the pending
delivery state, the frozen ten/ten eligibility rule, first-fit feasibility,
tree seal, and model-server release.

The shared activation snapshot carries inherited descriptive minima of four
sources and three domains. These fields did not govern Keystone admission; the
runner and independent auditor enforced ten and ten. Future apparatus should
parameterize this metadata so diagnostics and admission rules cannot appear to
conflict.

Supported:

* authentic result-delivery pressure after nine clean actor calls;
* feasible single-result deterministic relief; and
* non-attainment of the frozen interaction gate.

Not supported:

* bounded-causal treatment utility;
* construction, verification, repair, recheck, readiness, or closure effects;
* lowering the evidence threshold;
* a measured V0/V1 continuation from this fork; or
* further GPU authority.

Disposition: **AF/LR/NQ** — the run is valid and informative, but the fork is
non-qualifying. Offline activation-geometry reconciliation is the only selected
successor; no GPU operation is authorized.
