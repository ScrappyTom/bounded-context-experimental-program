# E76 — Orchard phase-lifecycle interaction result

Date: 2026-08-27

Standalone result commit:
`5bfd615` in `ScrappyTom/qwen38-artifact-coupled-integration-scout-v0`

Run ID: `2026-08-27-orchard-phase-lifecycle-measured-v0`

Frozen execution commit:
`094bbce57407568d1ef0ecd94414ae1a957e3b45`

## Literal result

The authorized F0-then-P1 run completed without runner failure:

| Measure | F0 fixed scaffold / append-only | P1 phase-conditional current verification | Total |
|---|---:|---:|---:|
| Actor calls | 14 | 24 | 38 |
| Maintenance calls | 8 | 8 | 16 |
| Provider calls | 22 | 32 | 54 |
| Serialized tokens | 322,315 | 414,017 | 736,332 |
| Candidate mutations | 9 | 10 | 19 |
| Current checks | 1 | 2 | 3 |
| Submitted | no | no | — |

One attempt and zero retries were used. Both cells and the aggregate run are
sealed; provider receipts are valid; both GPU runtimes were released. The
standalone suite passes 192 tests.

## Common construction system

Both cells were identical through actor call 12 and eight maintenance calls.
They shared:

* authentic first-fit pressure relief;
* fallible anchored-provenance maintenance;
* per-claim admission and unchanged-state fallback;
* a bounded twenty-claim scaffold;
* an exact thirteen-row evidence matrix;
* eight exact section-sized decision mutations; and
* the actor-selected `begin_verification` milestone.

The common trajectory is a fresh-world positive signal for the interaction
among semantic scaffolding, exact task-native ledger state, incremental artifact
actions, and bounded response transport. It is not an isolated register effect
or independent replication because the cells share the same seed and prefix.

## F0 disposition

F0 retained the scaffold and ordinary verification history:

```text
current check
read CURRENT + ASSAY
context pressure without feasible relief
```

It stopped after one post-check actor decision. Its exact candidate is a
1,075-word partial with five substantive groups met, seven partial, and nine
mechanical blockers. It is independently `not_ready`.

## P1 disposition

P1 externalized the scaffold and replaced the model-facing verification frame:

```text
current check
targeted CHILL/CURRENT/ASSAY reads
rejected patch
targeted CHILL read
admitted patch
effect uptake
current recheck
rejected patch
four byte-identical CURRENT reads
verification-call budget exhausted
```

The first verification prompt fell from F0's 19,010 tokens to 7,580. P1 reached
an admitted repair, delivered its effect into another actor decision, and ran a
current recheck. Mechanical blockers fell from nine to three.

Direct source-bound review finds all twelve substantive requirement groups met.
The remaining R05 mechanical failure is a surface-order false negative; the
artifact preserves the 82 C current WFI state and the every-return 80 C,
three-window gate. The true remaining defects are:

* 1,365 words versus the required 1,400–1,900; and
* three exact ledger source citations versus the required eight.

P1 is `strong_partial`, `not_ready`, and not a useful completion.

## Systems interpretation

The complete P1 allocation transfers positively for verification operability
and exact repair quality:

```text
temporary construction scaffold
+ exact incremental task artifact
+ actor-selected phase transition
+ scaffold demotion
+ bounded current candidate/effect/check projection
    -> deeper feedback loop
    -> repair effect uptake
    -> current recheck
    -> stronger exact artifact
```

It also exposes the next interaction failure. The second non-unique patch
rejection was visible in the immediately following call, but a subsequent
CURRENT observation displaced it as the latest update. The current frame did
not explicitly retain recurrence facts. The deterministic actor then requested
the same exact CURRENT range four times despite receiving it each time.

The comparison therefore identifies two bad extremes:

```text
retain too much path
    -> current verification becomes physically unreachable

retain current state with too little bounded recent causality
    -> current verification remains reachable but repeats
```

The target is not another permanent semantic note or a return to append-only
history. It is a complete bounded lifecycle whose verification frame couples:

* exact current artifact;
* current candidate/effect/check bindings;
* a small mechanical causal tail for latest action, rejection/result, and
  recurrence;
* uniquely bindable repair transport;
* exact history and source handles; and
* independent readiness and closure.

This target is an interaction hypothesis. No causal-tail field or repair format
has earned isolated promotion.

## Claim disposition

| Claim | Status |
|---|---|
| Orchard scaffold-plus-artifact construction works under pressure | supported locally |
| Fixed scaffold plus append-only verification is sustainable | local negative |
| P1 extends the verification feedback loop | supported locally |
| P1 enables repair effect uptake and current recheck | supported locally |
| P1 improves artifact quality over F0 | supported locally |
| P1 reaches readiness, submission, or useful completion | no |
| Memoryless current-state replacement is sufficient | local negative |
| Bounded mechanical causal continuity prevents recurrence | untested |
| Scaffold demotion alone caused the advantage | not identified |
| General architecture promotion | not justified |

## Next routing rule

Do not extend or tune this Orchard donor. Do not run a causal-tail-only prompt
ablation.

The next live study, if selected, must use a fresh task/world and compare
complete viable verification-lifecycle systems. It must preserve the now-earned
construction interaction, make repair structurally bindable, prospectively
bound a small host-derived causal tail, and continue through current recheck,
readiness, and closure. The causal question is whether bounded current state
plus bounded exact recent causality avoids both F0's capacity failure and P1's
repeat loop.

No new GPU run is authorized by this result.
