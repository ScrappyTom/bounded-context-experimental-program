# E75 — Orchard phase-lifecycle measured-run freeze

Date: 2026-08-27

Source:
`ScrappyTom/qwen38-artifact-coupled-integration-scout-v0@094bbce`

## Frozen comparison

The measured runner now hydrates the exact audited E74 fork. Both cells apply
the same first-fit relief and the same fallible anchored maintenance whenever a
source observation becomes external. Both retain the same exact incremental
evidence matrix and decision, seeds, action geometry, effect uptake, and
mechanical construction milestone.

The actor cannot check or submit during construction. It must invoke
`begin_verification` after the decision has the frozen title and headings, at
least 900 words, and at least seven distinct exact source citations. That event
changes phase but asserts neither quality nor readiness.

Check and submission are verification-only.

Only then do the complete systems diverge:

```text
F0_FIXED_SCAFFOLD_APPEND_ONLY_VERIFICATION
  retain frozen construction scaffold
  retain exact artifact
  append verification actions/effects/checks

P1_PHASE_CONDITIONAL_CURRENT_VERIFICATION
  externalize scaffold behind an exact hash-bound handle
  retain exact artifact
  replace candidate/effect/check projection after every verification action
  retain exact event history externally
```

This is deliberately compound. It cannot isolate scaffold demotion from current-
verification replacement.

## Qualification

The live handoff, inert authorization gate, 72 actor-call ceiling, 24
maintenance-call ceiling, 96 total provider-call ceiling, task/model locks, and
absent run directory pass offline preflight. A provider-free integration test
drives both runner paths through common relief and maintenance, construction,
phase transition, defect-bearing check, bounded repair, stale-check handling,
current recheck, and submission. The complete standalone suite passes 175
tests.

## Disposition

**AF/NQ** — zero new provider calls. The compound runner is reachable and
frozen; no comparative model behavior, artifact utility, readiness, or economic
claim is established. The next eligible operation is the separately authorized
F0-then-P1 measured run at the exact frozen commit.
