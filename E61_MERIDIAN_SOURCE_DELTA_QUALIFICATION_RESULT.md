# E61 — Meridian source-local-delta expression qualification result

Date: 2026-08-25

Status: measured one-call gate complete; execution and material safety passed;
transport and overall qualification failed; measured W0/L1 continuation closed

Source:
`ScrappyTom/qwen38-artifact-coupled-integration-scout-v0@ebd9d6ed773734258d6b75a460a467b11c88bc67`

## Literal result

The single authorized call finished normally. Its 4,234-token prompt produced
1,010 completion tokens and a 1,009-token admitted-form candidate. The output
contained exactly one AXIOM block and one BRAMBLE block, correct source
versions, all required local headings, and no readiness authorization.

The frozen transport validator rejected the output because the BRAMBLE block
mentioned DRIFT, EMBER, HEATH, and NORTH. One attempt was made, no retry was
available, and the runtime was released. The sealed run and independent audit
pass. Total serialized cost was 5,244 tokens.

## Material-safety result

Direct exact comparison with the two supplied sources found no material
reversal, fabricated exact value, wrong unit or authority, stale-version
promotion, probability conversion, or self-authorized readiness. The output
preserved the exact authority boundaries, emergency-procurement controls,
handoff currentness, 0.25/0.50 EU/mL distinction, 0.31/0.34 observations,
open-investigation hold, 41 percent assay-drift meaning, and candidate-bound
sample staleness.

Material safety therefore passed. That does not override failed transport or
establish completeness, actor use, artifact quality, or economic value.

## Systems finding

All four rejected identities occur literally in exact BRAMBLE as objects of
BRAMBLE-owned governing relationships. The output created no block for those
sources and attributed the relationship claims to BRAMBLE. The validator
nonetheless treated any known absent-source identity as an absent-source write.

This exposes an incompatibility in the tested interface:

```text
preserve cross-source relationships
        ×
forbid every lexical mention of a non-input source
        ↓
grounded relational content is unrepresentable
```

Bluehaven motivated strict source scope because its global ledger invented
content under unseen source slots. Meridian shows that slot ownership and
relationship-object reference are different relations. A safe source-local
operation must bind every claim to its exact owner source without necessarily
erasing the identities of objects that owner source discusses.

## Disposition

The prospective gate required both transport and material safety. It failed,
so the current L1 route and measured W0/L1 fork are ineligible. Do not retry,
relax the allowlist, edit the prompt, or re-admit this output on the same
boundary.

A future system may prospectively distinguish:

- source-slot owner: must be an exact current input source;
- relationship-object identity: may be referenced only if it occurs in the
  exact owner bytes; and
- source attribution: may not claim absent-source custody or create an absent
  source slot.

That would be a new provenance interface requiring a fresh frozen design and
activation, not a repair of E61.

Disposition: **LR/AF/NQ** — bounded source-local meaning was materially safe
but failed the frozen carrier because the safety rule collapsed provenance
ownership into lexical isolation. The whole-system utility question remains
unanswered; the current route is closed.
