# Completed experiment: pressure-qualified lifecycle scout v1

Status: completed historical contract; no further inference authorized

Result repository:
[`qwen38-pressure-qualified-lifecycle-scout-v1` at `e4dcd58`](https://github.com/ScrappyTom/qwen38-pressure-qualified-lifecycle-scout-v1/commit/e4dcd58fa554e7e36b99e6cb454803cd099d2044)

## Result disposition

The study found an authentic fresh-task result-delivery overflow. Deterministic
first-fit substitution admitted the pending result and repeatedly maintained
feasibility. Seed 314159 v2 has the separately qualified primary cell and
aggregate. Seed 43's action/lifecycle history is post-freeze replay-qualified,
while its primary v1 cell and aggregate remain sealed-aborted. Each 18-call
history had ten later pressure events, sixteen substitutions, and 80,497
cumulative recovered prompt tokens. Their model-facing action sequences were
identical at this one donor boundary.

The actor made sixteen novel source reads and two rejected over-range reads per
continuation, with no reopen, mutation, check, or submission. The actor-call
budget ended while required evidence remained incomplete; context capacity did
not terminate the run. The two seeds are not independent-world recurrence.

This qualifies first-fit relief as a narrow mechanical pressure instrument and
characterizes a forward-streaming acquisition regime. It does not establish
artifact utility, semantic continuity, acquisition-stopping failure,
convergence, or a general runtime policy. No longer-horizon successor is
selected.

## Frozen question

At a fresh ordinary trajectory's first authentic result-delivery overflow, does
the reactive runtime policy improve useful completion relative to the same
exact pre-pressure history without lifecycle intervention?

## Qualification

Accessible-world size is insufficient. A non-measured screening seed must
produce a requested exact result whose prospective delivery violates the frozen
prompt/reserve limit. The task may not be modified after screening. The screen
seed is not used in the measured comparison.

The preferred design freezes and forks the exact ordinary trajectory at that
boundary. Treatment and control are identical before pressure. Control reaches
the ordinary capacity endpoint. Treatment applies deterministic first-fit
relief and continues; reactive reentry is permitted only if the complete frozen
eligible relief class remains insufficient.

## Required Stage 0 corrections

- task-specific deterministic model-visible check diagnostics with exact raw
  custody;
- observation-to-candidate current/stale bindings;
- live qualification of a strict one-action interface;
- a minimum plausible successful path including effect uptake, review/repair,
  and closure;
- a prospective protocol-error allowance without retries;
- exact response/control reserve objects;
- independent candidate-bound evaluation/readiness for both arms;
- no treatment-only investigator-selected evidence.

The completed scout's 19/71 rejection rate and nine-call research ceiling are
nuisance evidence for design, not parameters to copy automatically.

## Interpretation

A positive result supports the complete reactive policy in the selected task
and seeds. It does not establish component necessity. A valid negative result
receives its frozen replication and routes the next mechanism from the first
new exact failure boundary. Survival alone is not useful completion, and false
closure is a loss.

The original authorization was consumed by the sealed execution chain. This
document does not authorize a rerun or extension. The active program direction
is `STRUCTURED_EXPLORATION_ROADMAP.md`.
